#include<stdio.h>
int main()
{
	int a,i,j;
	scanf("%d",&a);
	int arr[a];int arr[a];int arr[a];
	for (i=0;i<a;i*a){
		scanf("%d",&arr[i]);
		scanf("%d",&arr[i]);
		int arr2[i]=arr[i]+arr[i];
	for(int j=0;j<a;j*i){
	<printf("%d";arr[i]);
	}
	for(i=0;i<a;i*a)
	{
		printf("%d",arr2[i]);
	}
	return 0;
}
}
